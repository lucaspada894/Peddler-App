spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:mysql://proj309-pp-07.misc.iastate.edu:3306/PedBase?autoReconnect=true&useSSL=false
spring.datasource.username=teampp7
spring.datasource.password=PP7_peddler
server.port=8080

# Web
spring.thymeleaf.mode=HTML


# Internationalization
spring.messages.basename=messages/messages

# Actuator / Management
management.endpoints.web.base-path=/manage
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
spring.resources.cache.cachecontrol.max-age=12h


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB

# Max file size.
spring.servlet.multipart.max-file-size=200MB

# Max Request Size
spring.servlet.multipart.max-request-size=215MB

#max http request size
server.max-http-header-size=10000000 

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/var/www/html/uploads



#spring.datasource.url=jdbc:mysql://proj309-pp-07.misc.iastate.edu:3306/ys?verifyServerCertificate=false&useSSL=false&requireSSL=false